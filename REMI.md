# REMI Work Log

Этот файл фиксирует только выполненную мной работу.

## 2026-04-09

- Поднял инфраструктуру в Docker (`db`, `airflow-init`, `airflow-scheduler`, `airflow-webserver`).
- Починил запуск compose в папке с кириллицей через явное имя проекта (`name: neo_hack`).
- Настроил порт PostgreSQL на хосте через `.env` (`POSTGRES_PORT=5433`) из-за конфликта 5432.
- Подтянул изменения ветки `emil`.
- Запустил загрузку raw данных MOEX для `SBER` в `stg.raw_moex_data`.
- Проверил загрузку данных SQL-запросами в БД.
- Запустил трансформацию `stg.raw_moex_data -> core.minute_candles` для одного тикера и одного дня.
- Проверил построение агрегатов `core.hourly_candles`, `core.daily_candles`, `core.weekly_candles`.
- Подтвердил корректность логики интервалов:
  - `open` — первая минута интервала,
  - `close` — последняя минута интервала,
  - `high`/`low` — максимум/минимум за интервал,
  - `volume` — сумма объема за интервал.
